/**
 * league/index.ts — the public face of the league domain.
 *
 * Pure: times and counts in, leagues and zones out. No React, no storage, no
 * network. A server computing the same standings would import exactly this.
 */

export * from './league';
